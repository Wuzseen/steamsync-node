/**
 * Created by Aivis on 17/02/2016.
 */

import React from 'react';
//import ReactDOM from 'react-dom';
import Simpletable from '../../src/simpletable.jsx';

window.onload = () => {
    var container = document.querySelector('#container');
    var simpletable;

    simpletable = React.render(
        <SimpleTable
            url="data.json"
            pollInterval={2000}
            addButton="true"
        />,
        container
    );
};